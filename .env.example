# Copy this file to `.env` and fill in the values. `.env` is gitignored.

# Required: get one from https://console.anthropic.com/
ANTHROPIC_API_KEY=

# Optional: override the path to the jobtrack repo. Default resolves to the
# sibling directory `../application-tracker` next to this repo.
# JOBTRACK_PROJECT_PATH=
